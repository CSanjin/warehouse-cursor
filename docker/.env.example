# 复制为项目根目录 .env 后使用：copy docker\.env.example .env
# 数据安全：敏感信息通过环境变量注入，不写入镜像

MYSQL_ROOT_PASSWORD=warehouse123
MYSQL_USER=warehouse
MYSQL_PASSWORD=warehouse123

# 对外暴露端口（可按宿主机情况调整）
WEB_PORT=80
GATEWAY_PORT=8070
NACOS_PORT=8848
SENTINEL_PORT=8858
